model User {
  id   String @id @default(ulid())
  name String @db.Varchar(255)
  email String @unique
  password String
  activeStatus ActiveStatus @default(ACTIVE)

}

enum ActiveStatus {
  ACTIVE
  BLOCKED
}
